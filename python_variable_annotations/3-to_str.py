#!/usr/bin/env python3
"""Convert an integer to a string. function: to_str(n: int) -> str"""


def to_str(n: int) -> str:
    """Convert an integer to a string. function to_str(n: int) -> str"""
    return str(n)
