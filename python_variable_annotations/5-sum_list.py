#!/usr/bin/env python3
"""Sum a list of numbers and return the result."""

from typing import List


def sum_list(input_list: List[float]) -> int:
    """Sum a list of numbers and return the result."""
    return sum(input_list)
